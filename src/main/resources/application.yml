server:
  port: 8044

spring:
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/yourdb
    username: postgres
    password: ****